import React from 'react';
import Header from './Header';

export default function Standings() {
    return (
        <div>
            <Header/>
            <h1>Standings</h1>
        </div>
    );
}